import ClientPage from "./ClientPage";

export const metadata = {
     title: 'InstaTrack | Self-Hosted Instagram Analytics',
     description: 'A privacy-focused Instagram analytics dashboard that detects ghost followers, visualizes network graphs, and tracks engagement without third-party servers.',
};

export default function Page() {
     return <ClientPage />;
}
